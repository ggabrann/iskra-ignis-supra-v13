# Архитектура
Монорепо: apps, packages, infra, docs. Core — символы/ритуалы/SLO/память.
API — агрегаты only (opt-in). Фрактальная память как граф связей (LINKMAP).
Инсайты/профиль — легкие артефакты без PII.
